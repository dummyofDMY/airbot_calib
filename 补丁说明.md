# 相机标定补丁

## 概述

本仓库存储的是官方抓取demo的修改版本，主要优化了一些标定的流程。

## 修改内容概述

- 将角点检测算法 `findChessboardCorners` 替换为了 `findChessboardCornersSB`，并配置了有关参数。后者在检测图片上标定板较小、视角较倾斜、镜头畸变较大时有更强的鲁棒性
- 加入离线模式，参数的计算利用通过上一次采集的数据，由 `--input-dir` 参数指定数据路径
- 加入数据审查，录制或加载完数据后会要求逐张审查角点检测结果，按 `d` 键丢弃当前数据，其余任意键接受
- 加入外参正确性验证，其会计算当前手眼参数下各帧数据算出来的标定板相对机械臂基座的位姿，并根据这个位姿一致性程度评估外参、数据的质量
